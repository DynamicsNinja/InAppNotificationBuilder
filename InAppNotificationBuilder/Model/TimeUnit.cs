namespace Fic.XTB.InAppNotificationBuilder.Model
{
    public enum TimeUnit
    {
        Seconds = 1,
        Minutes = 60,
        Hours = 3600,
        Days = 86400,
        Weeks = 604800 
    }
}